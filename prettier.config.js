const config = {
  singleQuote: true,
  semi: true,
  tabWidth: 2,
  useTabs: false,
  arrowParens: 'avoid',
  trailingComma: 'es5',
  plugins: ['prettier-plugin-organize-imports'],
};

module.exports = config;
